# StreamX - Live Scores

موقع نتائج مباشرة بدون CORS باستخدام Cloudflare Worker

## النشر
1. ارفع `index.html` على GitHub Pages / Vercel / Netlify
2. انشر الـ Worker: `npx wrangler deploy`
3. انسخ رابط الـ Worker وضعه في `WORKER_URL` داخل `index.html`
